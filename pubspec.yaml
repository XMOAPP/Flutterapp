name: xmo
description: XMO Messaging App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  google_fonts: ^6.1.0
  provider: ^6.1.1
  cupertino_icons: ^1.0.6
  matrix: ^0.25.0
  hive_flutter: ^1.1.0
  path_provider: ^2.1.0
  firebase_core: ^4.7.0
  firebase_auth: ^6.4.0
  mailer: ^7.1.0
  http: ^1.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
